<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zfani.assaf.jobim">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/icon" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/orange" />

        <service android:name=".Services.FirebaseMessagingService">

            <intent-filter>

                <action android:name="com.google.firebase.MESSAGING_EVENT" />

            </intent-filter>

        </service>

        <activity android:name=".Activities.LoadingPage">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <activity
            android:name=".Activities.HomePage"
            android:windowSoftInputMode="adjustNothing" />

        <activity
            android:name=".Activities.ShowBy"
            android:windowSoftInputMode="adjustNothing" />

        <activity android:name=".Activities.JobInfo" />

        <activity android:name=".Activities.JobsEmployer" />

        <activity
            android:name=".Activities.FillDetails"
            android:windowSoftInputMode="adjustNothing" />

        <activity
            android:name=".Activities.AddNewJob"
            android:windowSoftInputMode="adjustNothing" />

        <activity
            android:name=".Activities.FilterQuestion"
            android:windowSoftInputMode="adjustNothing" />

        <activity
            android:name=".Activities.MakingContact"
            android:windowSoftInputMode="adjustNothing" />

    </application>

</manifest>